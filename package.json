{
  "name": "ca-xray",
  "version": "1.0.0",
  "private": true,
  "description": "Evidence-based crypto contract forensic analysis",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "db:migrate": "node scripts/migrate.js",
    "db:rollback": "node scripts/rollback.js"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
